<Window x:Class="PokemonCardManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PokemonCardManager"
        xmlns:views="clr-namespace:PokemonCardManager.Views"
        mc:Ignorable="d"
        Title="Pokémon Card Manager" Height="720" Width="1280" 
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundColor}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Moderna con Gradiente -->
        <Border Background="{StaticResource SidebarGradient}" Grid.Column="0">
            <Border.Effect>
                <DropShadowEffect Color="#000000" 
                                Direction="0" 
                                ShadowDepth="4" 
                                Opacity="0.15" 
                                BlurRadius="16"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Intestazione con Logo -->
                <Border Grid.Row="0" Margin="20,30,20,30" Padding="0">
                    <StackPanel>
                        <TextBlock Text="🎴" 
                                   FontSize="36" 
                                   HorizontalAlignment="Center" 
                                   Margin="0,0,0,10"/>
                        <TextBlock Text="Pokémon Card" 
                                   Foreground="White" 
                                   FontSize="20" 
                                   FontWeight="Bold" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,2"/>
                        <TextBlock Text="Manager" 
                                   Foreground="#FFFFFFAA" 
                                   FontSize="14" 
                                   FontWeight="Medium" 
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Pulsanti di navigazione -->
                <StackPanel Grid.Row="1" Margin="0,10,0,20">
                    <Button x:Name="btnInventory" 
                            Content="📦 Inventario" 
                            Style="{StaticResource MenuButtonStyle}" 
                            Foreground="White"
                            Click="NavigateToInventory"/>
                    
                    <Button x:Name="btnSales" 
                            Content="💰 Vendite" 
                            Style="{StaticResource MenuButtonStyle}" 
                            Foreground="White"
                            Click="NavigateToSales"/>
                    
                    <Button x:Name="btnDashboard" 
                            Content="📊 Dashboard" 
                            Style="{StaticResource MenuButtonStyle}" 
                            Foreground="White"
                            Click="NavigateToDashboard"/>
                    
                    <Button x:Name="btnSettings" 
                            Content="⚙️ Impostazioni" 
                            Style="{StaticResource MenuButtonStyle}" 
                            Foreground="White"
                            Click="NavigateToSettings"/>
                </StackPanel>

                <!-- Footer -->
                <Border Grid.Row="2" 
                        BorderBrush="#FFFFFF30" 
                        BorderThickness="0,1,0,0" 
                        Margin="20,0,20,20" 
                        Padding="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="v1.0.0" 
                                   Foreground="#FFFFFF80" 
                                   HorizontalAlignment="Center" 
                                   FontSize="12"/>
                        <TextBlock Text="© 2024" 
                                   Foreground="#FFFFFF60" 
                                   HorizontalAlignment="Center" 
                                   FontSize="11" 
                                   Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Contenuto principale -->
        <Grid Grid.Column="1" Background="{StaticResource BackgroundColor}">
            <Frame x:Name="MainFrame" 
                   NavigationUIVisibility="Hidden"
                   Background="Transparent"/>
        </Grid>
    </Grid>
</Window>
